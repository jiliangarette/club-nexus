// Configures PostCSS to use Tailwind CSS for utility-first styling and Autoprefixer for adding vendor prefixes to CSS for cross-browser compatibility.

export default {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
};
